Create a basic application using .NET Core / ASP.NET Core that integrates with the Napster API.

Requirements:

Use C# and the latest stable version of .NET.
Create a simple, clean project structure.
Integrate with the Napster API using HttpClient.
Store the Napster API key securely using configuration/environment variables.
Implement a basic service layer for communicating with Napster.
Add functionality to:
Search for artists, albums, and tracks.
Retrieve basic artist information.
Retrieve an artist's albums.
Retrieve tracks for an album.
Create simple REST API endpoints for these operations.
Use dependency injection for services.
Add proper error handling for API failures and invalid requests.
Use async/await for API calls.
Create DTOs/models for the API responses rather than exposing raw API responses directly.
Include basic logging.
Add a few unit tests for the service/controller logic.

Please provide:

The project structure.
All required NuGet packages.
Complete source code for each file.
Configuration setup, including where to put the Napster API key.
Example API requests and responses.
Instructions for running the application locally.

Keep the implementation beginner-friendly and avoid unnecessary complexity. Explain the important parts of the code briefly.