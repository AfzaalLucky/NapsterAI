VITE_API_BASE_URL=http://localhost:5080/api

# AI chat widget (RealEstateChatWidget.tsx) - id of the Napster companion configured as the
# "Real Estate AI Sales Agent" persona. Runbook: open NapsterAI.Playground (npm run dev in
# src/NapsterAI.Playground), pick or create the companion for this persona, then use its
# "Agent" tab to save real-estate-appropriate instructions/knowledge base/FAQs against it -
# copy that companion's id here. Left blank, the widget doesn't render (see the component for
# the exact fallback behavior) rather than erroring for visitors.
VITE_REAL_ESTATE_COMPANION_ID=a81e8618-4530-4a72-9cd5-f7b644a4e29a
VITE_REAL_ESTATE_VOICE_ID=alloy
